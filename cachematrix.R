#----------------------------------------------------------------#
#  R Programming - Assignment 2                                  #
#                                                                #
# This function creates a special matrix object that can         #
# cache its inverse                                              #
#                                                                #
#----------------------------------------------------------------#

makeCacheMatrix <- function(x = matrix()) {















}

#------------------------------------------------------------------#
## This function tests if the inverse matrix exists                #
## if exists, give a message and returns the Inverse Matrix cached #
## if not, calculates the solv of the matrix                       #
#------------------------------------------------------------------#

cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'











#--  inverse the matrix 

   m<-solve(data)

#--  m is the R object that contains the inverse matrix and is the product of the function

   m
}
